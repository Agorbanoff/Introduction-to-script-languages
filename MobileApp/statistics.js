import React from "react";
import { StatusBar } from 'expo-status-bar';
import {
  StyleSheet,
  Text,
  View,
  SafeAreaView,
  TextInput,
  TouchableOpacity,
  ImageBackground,
} from 'react-native';

export default function StatisticsPage(){

    return(
        <View>
            <SafeAreaView> 
                <Text>This is statistics page</Text>
            </SafeAreaView>
        </View>
    );
}
